version: 2.1
jobs:
  build:
    docker:
      - image: ruby:3.1.2
    steps:
      - checkout
      - run:
          name: Build ruby version & test
          command: |
            cd ruby
            gem install bundler -v 2.3.20
            bundle install
